org.gradle.jvmargs=-Xmx**JVM_HEAP_SIZE**M
org.gradle.parallel=true
unityStreamingAssets=**STREAMING_ASSETS**
# Android Resolver Properties Start
android.useAndroidX=true
android.enableJetifier=true
# Android Resolver Properties End
# AGP 9.0 flipped android.uniquePackageNames to true by default.
# googlemobileads-unity.aar and GoogleMobileAdsPlugin.androidlib both declare
# the namespace com.google.unity.ads, which is now a hard error instead of a warning.
# Opt out until the GoogleMobileAds plugin ships unique namespaces.
android.uniquePackageNames=false
**ADDITIONAL_PROPERTIES**
